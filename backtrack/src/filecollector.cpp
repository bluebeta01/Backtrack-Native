#include <filesystem>
#include "filecollector.h"
#include "path.h"

std::vector<std::string> FileCollector::collectFiles(const ArchiveConfig& archiveConfig)
{
	for (const FileOrigin& origin : archiveConfig.origins)
	{

	}
}
